# Team7Chat
> This Chat were created during hackaton in DevEducation. The client’s wish was to create a chat where users will be registered by ip, as well as be able to communicate with each other, receive and send messages, change the color of messages, change the username, etc.

## Technologies
* React Native

## Setup
Clone project from repository
run:

npm install

run: 

cd ios && pod install && cd ..

run for IOS:

react-native run-ios 

run for Android:

react-native run-android

